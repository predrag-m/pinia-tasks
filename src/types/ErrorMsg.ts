export const enum ErrorMsg {
  noData = "No data available.",
  postTask = "Couldn't POST the task to the DB.",
  deleteTask = "Couldn't DELETE the task from the DB.",
  patchTask = "Couldn't PATCH the task in the DB.",
  notInStore = "Pinia store can't find the task with id:",
}
